# HexStrike AI v3.0 - Simple MCP Requirements
# Minimal dependencies for Python MCP script

# No external dependencies required
# simple_mcp.py uses only Python standard library:
# - json (built-in)
# - subprocess (built-in) 
# - sys (built-in)
# - datetime (built-in)

# All security tools run inside Docker container